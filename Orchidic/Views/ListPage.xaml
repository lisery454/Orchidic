<UserControl x:Class="Orchidic.Views.ListPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:utils="clr-namespace:Orchidic.Utils"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:models="clr-namespace:Orchidic.Models"
             xmlns:viewModels="clr-namespace:Orchidic.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:ListPageViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
  <UserControl.Resources>
    <viewModels:AudioListCountTextConverter x:Key="AudioListCountTextConverter" />
  </UserControl.Resources>

  <Grid HorizontalAlignment="Stretch" Margin="0,100,0,100" VerticalAlignment="Stretch" MaxHeight="1000">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="5*" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Grid Grid.Column="1">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <Grid Grid.Row="0" Margin="0,0,0,10">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" FontSize="16"
                   Margin="0,0,15,0"
                   Text="{Binding AudioListService.AudioLists.Count, Converter={StaticResource AudioListCountTextConverter }}">
        </TextBlock>
      </Grid>


      <Grid Grid.Row="1" Margin="0,0,0,15">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" FontSize="16" Text="搜索" Margin="0,0,15,0" HorizontalAlignment="Stretch"
                   VerticalAlignment="Center" />
        <TextBox Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Center"
                 FontSize="16"
                 Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" />

      </Grid>


      <ListBox
        Grid.Row="2"
        ItemsSource="{Binding AudioListsView}"
        Background="Transparent"
        HorizontalAlignment="Stretch"
        BorderThickness="0"
        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
        ScrollViewer.VerticalScrollBarVisibility="Auto"
        FocusVisualStyle="{x:Null}"
        AllowDrop="True"
        ScrollViewer.CanContentScroll="True">

        <ListBox.ItemsPanel>
          <ItemsPanelTemplate>
            <VirtualizingStackPanel />
          </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
        <ListBox.Resources>
          <Style TargetType="ListBoxItem">
            <Setter Property="Margin" Value="0 2" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
              <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                  <Border x:Name="Border"
                          HorizontalAlignment="Stretch"
                          Background="Transparent"
                          Padding="0,0,5,5">
                    <ContentPresenter HorizontalAlignment="Stretch" />
                  </Border>
                  <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                      <Setter Property="Background" Value="{StaticResource SecondaryBrush}"></Setter>
                    </Trigger>
                  </ControlTemplate.Triggers>
                </ControlTemplate>
              </Setter.Value>
            </Setter>
          </Style>

          <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="8" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
              <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                  <Grid>
                    <Track x:Name="PART_Track" IsDirectionReversed="True">
                      <Track.DecreaseRepeatButton>
                        <RepeatButton
                          Style="{x:Null}" Background="Transparent" BorderBrush="Transparent"
                          OverridesDefaultStyle="True" FocusVisualStyle="{x:Null}" />
                      </Track.DecreaseRepeatButton>
                      <Track.Thumb>
                        <Thumb x:Name="Thumb" Style="{StaticResource CustomThumb}" />
                      </Track.Thumb>
                      <Track.IncreaseRepeatButton>
                        <RepeatButton
                          Style="{x:Null}" Background="Transparent" BorderBrush="Transparent"
                          OverridesDefaultStyle="True" FocusVisualStyle="{x:Null}" />
                      </Track.IncreaseRepeatButton>
                    </Track>
                  </Grid>
                </ControlTemplate>
              </Setter.Value>
            </Setter>
          </Style>
        </ListBox.Resources>
        <ListBox.ItemTemplate>
          <DataTemplate>
            <Grid Margin="0">
              <Border CornerRadius="10" Padding="10,10" Height="60" Margin="0,0,0,0"
                      HorizontalAlignment="Stretch" Background="{StaticResource MenuBGBrush}">
                <b:Interaction.Triggers>
                  <b:EventTrigger EventName="MouseLeftButtonUp">
                    <b:InvokeCommandAction
                      Command="{Binding DataContext.LoadListCommand, RelativeSource={RelativeSource AncestorType=UserControl }}"
                      CommandParameter="{Binding}" />
                  </b:EventTrigger>
                </b:Interaction.Triggers>

                <Grid d:DataContext="{d:DesignInstance Type=models:AudioList}">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <!-- <ColumnDefinition Width="Auto" /> -->
                  </Grid.ColumnDefinitions>

                  <TextBlock Grid.Column="1"
                             Text="{Binding  Name}"
                             Margin="5 0 10 0"
                             TextTrimming="CharacterEllipsis"
                             FontSize="16"
                             TextWrapping="Wrap"
                             HorizontalAlignment="Stretch"
                             VerticalAlignment="Center">
                    <TextBlock.Style>
                      <Style TargetType="TextBlock" BasedOn="{StaticResource TextBlockBaseStyle}">
                        <Setter Property="Foreground" Value="{StaticResource FontBrush}" />
                      </Style>
                    </TextBlock.Style>
                  </TextBlock>

                  <Grid Grid.Column="2">
                    <Button Height="30" Width="30" Style="{DynamicResource AnimatedButton}"
                            Command="{Binding DataContext.RemoveListCommand, RelativeSource={RelativeSource AncestorType=UserControl }}"
                            CommandParameter="{Binding}">
                      <utils:SuperellipseBorder Height="30" Width="30" CornerRadius="10" Power="8"
                                                Fill="{DynamicResource SecondaryBrush}" />
                    </Button>
                  </Grid>
                </Grid>

              </Border>
            </Grid>


          </DataTemplate>
        </ListBox.ItemTemplate>

      </ListBox>
    </Grid>
  </Grid>

</UserControl>